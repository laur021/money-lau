export type LedgerRow = { transaction_type: string; amount: number | string; currency: string; status: string; transaction_date: string; category_id: string | null };
export function completedRows(rows: LedgerRow[]) { return rows.filter((row) => row.status === 'completed'); }
export function totalsByCurrency(rows: LedgerRow[]) { return completedRows(rows).reduce<Record<string, { income: number; expense: number }>>((totals, row) => { const total = totals[row.currency] ?? { income: 0, expense: 0 }; if (row.transaction_type === 'income') total.income += Number(row.amount); if (row.transaction_type === 'expense') total.expense += Number(row.amount); totals[row.currency] = total; return totals; }, {}); }
export function toCsv(rows: LedgerRow[]) { return ['type,amount,currency,status,date', ...rows.map((row) => [row.transaction_type, row.amount, row.currency, row.status, row.transaction_date].map((value) => `"${String(value).replaceAll('"', '""')}"`).join(','))].join('\n'); }
